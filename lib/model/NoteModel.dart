class NoteModel {
  String title, description;
  int time_stamp, is_sync,is_update,delete_sync;

    NoteModel({this.title, this.description, this.time_stamp, this.is_sync, this.is_update,this.delete_sync});
}
